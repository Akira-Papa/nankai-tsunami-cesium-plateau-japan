# 動作確認・不具合修正 3サイクル

## 実データ統合前の確認
- 共有URLとUIの時間上限の不一致を検出。公式計算の12時間に合わせ0〜720分へ統一。opacity0も往復保持。
- 地理院淡色タイルのz0/1が404となる実ブラウザ不具合を検出。低倍率は標準地図に切り替え、日本範囲を指定して再確認。
- manifest失敗で操作UIまで止まる設計を修正。独立読込・地図操作継続・再試行ボタンを追加。
- 再試行ボタンが親要素pointer-events:noneで押せない問題を修正。
- 旧建物一律水面シェーダは明示的に無効化。建物は元の表示を保つ。
- 集約グリッドと描画矩形の緯度換算定数を統一。
- 自治体重心が山中になる静岡ショートカットを沿岸市街地へ修正。

## サイクル1
実データ2ケースを使い8項目すべて成功。全国6,288セル、名古屋3,091セル・100m表示・建物3件を実ブラウザ確認。モバイル390×844、到達再生・停止、共有復元、503障害後の旧結果除去と再試行を確認。pageErrors=0、externalFailures=0。証拠: evidence/cycle-1/report.json とPNG。

追加レビューで地点照会の境界許容幅が隣のセルまで含むこと、描画更新中に古い時刻のセルが残り得ることを指摘。サイクル2前に矩形半開区間による照会と、更新中の旧バッチ非表示・8バッチ/frame更新へ修正。集約セル全域が浸水した意味ではない説明も常時表示へ強化。

## サイクル2
本番用distをpreviewで配信し、9項目すべて成功。名古屋・高知・静岡・大阪の4都市で100m集約描画を確認。pageErrors=0。外部失敗18件は全て移動/再読込に伴うnet::ERR_ABORTED（PLATEAUの不要リクエスト中断）であり実HTTP障害とは分けて記録。

追加検査でGPU準備判定が未生成バッチを含まず早く成立することと、同一データ再取得で不要な再描画が発生することを発見。サイクル3前に未生成分もpendingへ加え、同一セル/同一表示条件の再描画を抑止。地点検索の境界修正も保持。

## サイクル3
本番用distで11項目すべて成功。4都市すべてGPU全バッチ準備完了(pending=0)を確認。manifestの503でも地図操作を継続し再試行で復旧。地形503では「平坦な地球・標高なし」と表示し、実計算データは保持。ケース503で旧セルを消し復旧することを再確認。pageErrors=0。外部中断61件の内訳はreport.json参照。

最終: TypeScript57テスト成功、Python集約2テスト成功、typecheck/build/git diff --check成功。dist68MB、原本CSV/ZIP・Markdown・秘密設定の混入なし。3サイクルの自動操作は合計28項目成功。
